<!--
  These AI instructions live in AGENTS.md -- the open convention every agent reads.
  Claude Code reads CLAUDE.md rather than AGENTS.md, so this file imports it.
  Keep this stub to the pointer + import: put content in AGENTS.md so all agents see it.
-->

**The instructions for this directory are in `src/components/AGENTS.md`. If its contents are not already
in your context, read that file now, before anything else.** The repo-wide instructions
in the root `AGENTS.md` apply too -- read that as well if it is not in your context.

Do not delete the pointer above. The `@` import below is expanded only when your working
directory *is* this one; from any subdirectory the import silently does nothing and the
pointer is the only thing carrying these instructions.

@AGENTS.md
