<!--
  This repo's AI instructions live in AGENTS.md -- the open convention every agent reads.
  Claude Code reads CLAUDE.md rather than AGENTS.md, so this file imports it.
  Keep this stub to the pointer + import: put content in AGENTS.md so all agents see it.
-->

**This project's instructions are in `AGENTS.md` at the repository root. If its contents
are not already in your context, read that file now, before anything else.**

The `@` import below is expanded only when your working directory *is* the repository root. Started
anywhere else, Claude Code loads this file verbatim and the import does nothing -- so
without the line above you would silently have no project instructions at all. Measured
2026-08-04 against Claude Code.

@AGENTS.md
